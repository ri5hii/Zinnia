{
	"name": "zinnia",
	"type": "module",
	"private": true,
	"engines": {
		"node": ">=24"
	},
	"packageManager": "bun@1.3.14",
	"scripts": {
		"pretest": "bun run build",
		"build": "tsup leetcode/packages/core/src/index.ts --format esm --out-dir leetcode/packages/core/dist --platform node --target node24 --sourcemap --clean && tsup trophy/src/renderer.ts --format esm --out-dir api/_build/trophy --platform node --target node24 --sourcemap --clean && tsup streak/src/index.ts --format esm --out-dir streak/dist --platform node --target node24 --sourcemap --clean && tsup streak/src/index.ts --format esm --out-dir api/_build/streak --platform node --target node24 --external sharp --minify --clean && tsup stats/src/index.ts --format esm --out-dir stats/api --platform node --target node24 --sourcemap --clean",
		"vercel:deploy": "vercel --prod --token $VERCEL_TOKEN --yes",
		"lint": "biome check . && tsc --noEmit",
		"format": "biome format . --write",
		"type-check": "tsc --noEmit",
		"test:stats": "vitest --run --config ./vitest.config.ts -- -t 'tests/stats'",
		"test:api": "vitest --run --config ./vitest.config.ts -- -t 'tests/api'",
		"test:leetcode": "cd leetcode && vitest --run --pool=threads",
		"test": "vitest --run",
		"e2e:local": "bun run build && (bunx vercel dev --listen 3000 & sleep 3) && bash scripts/e2e_smoke.sh http://localhost:3000"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"@vitest/coverage-v8": "^4.1.7",
		"jsdom": "^29.1.1",
		"tsup": "^8.5.1",
		"typescript": "^6.0.3",
		"vercel": "^54.6.1",
		"vitest": "^4.1.7"
	},
	"dependencies": {
		"@vercel/node": "^5.8.8",
		"dotenv": "^17.4.2",
		"emoji-name-map": "^2.0.3",
		"linkedom": "^0.18.12",
		"sharp": "^0.34.5"
	},
	"overrides": {
		"basic-ftp": "^5.2.0",
		"tar": "^7.5.7",
		"rollup": "^4.59.0"
	}
}
